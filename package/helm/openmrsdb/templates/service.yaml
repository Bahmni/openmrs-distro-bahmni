apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmrsdb.fullname" . }}
  labels:
    {{- include "openmrsdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openmrsdb.selectorLabels" . | nindent 4 }}
